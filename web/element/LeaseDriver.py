class LeaseDriver:
    def __init__(self, web_driver_handle):
        self.web_driver_handle = web_driver_handle

    def process_lease_listings(self, address_entry):
        pass
        # grab number of listings in the window to keep index track
        # for all the listings seen
            # click listing
            # check type:  !sublet or regus
            # grab type
            # grab rent
            # grab square
            # grab contacts
            # click the new Back button
            # refresh driver to grab the table